# LQR estimator-crossover: results

Generated by `scripts/lqr_crossover/report.py`. Decision rules are adjudicated against `docs/prereg_lqr_crossover.md` as committed; see its Sec. 1 for what was and was not outcome-seen at commit time.


## 0. Design as executed

| d | n | rho_closed | cond_H | tr_H2 | lyap_resid_rel | retries | eps/q_spread | alpha_Q | alpha_s | N | states |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 2 | 0.4517 | 1.00 | 12.2 | 1.3e-16 | 0 | 0.050 | 0.2858 | 2.2871 | 10000 | 64 |
| 2 | 4 | 0.5347 | 16.19 | 686.9 | 1.3e-16 | 0 | 0.050 | 0.0382 | 5.3511 | 10000 | 64 |
| 4 | 8 | 0.4709 | 7.49 | 118.6 | 3.2e-16 | 0 | 0.050 | 0.1042 | 3.7608 | 10000 | 64 |
| 8 | 16 | 0.6901 | 9.67 | 232.6 | 2.9e-15 | 0 | 0.050 | 0.0842 | 2.9171 | 10000 | 64 |
| 16 | 32 | 0.6050 | 6.52 | 241.8 | 4.9e-15 | 0 | 0.050 | 0.1233 | 2.0284 | 10000 | 64 |
| 32 | 64 | 0.5577 | 7.73 | 537.0 | 5.6e-15 | 0 | 0.050 | 0.1089 | 1.3720 | 10000 | 64 |
| 64 | 128 | 0.5176 | 8.76 | 1033.4 | 7.1e-15 | 0 | 0.050 | 0.0967 | 1.2129 | 10000 | 64 |

Guards (prereg Sec. 7): rho_closed <= 0.99 and cond_H <= 50 at every d. git_sha `33c76325a5`.


## 1. Gates

Run `python scripts/lqr_crossover/gates.py`; G6a/G6b run separately (`scripts/verify_estep.py`, `scripts/probe2_full_estimator.py`). The gate table is reproduced from those runs and is the precondition for everything below.


## 2. E0a -- the classical dimension factor

`(M-1) MSE_ZO / ||g*||^2 = d + 1` exactly, in the linear-critic limit. The spec's `MSE_ZO/MSE_PW` ratio does NOT grow linearly in d and cannot: `MSE_PW = 4 sigma^2 tr(H^2)/M` is nonzero and itself grows with d, so the ratio tends to a constant. See gates.py G8.


## 3. E1a -- the error-only crossover (primary)

| d | c* measured | c* analytic | sqrt(d M/(M-1)) | rel. diff | collapse sd (log units) | columns bracketed |
|---|---|---|---|---|---|---|
| 1 | 1.2014 | 1.2098 | 1.0160 | 6.95e-03 | 0.0078 | 20/20 |
| 2 | 1.5182 | 1.4897 | 1.4368 | 1.91e-02 | 0.0015 | 20/20 |
| 4 | 2.0495 | 2.0414 | 2.0320 | 4.01e-03 | 0.0009 | 20/20 |
| 8 | 2.8746 | 2.8741 | 2.8737 | 1.84e-04 | 0.0005 | 20/20 |
| 16 | 4.0603 | 4.0640 | 4.0640 | 9.06e-04 | 0.0005 | 20/20 |
| 32 | 5.7465 | 5.7474 | 5.7474 | 1.46e-04 | 0.0005 | 20/20 |
| 64 | 8.1306 | 8.1280 | 8.1280 | 3.20e-04 | 0.0008 | 20/20 |

**Fitted exponent** `log c* = const + p log d`, registered d-set [2, 4, 8, 16, 32, 64]: **p = 0.4876** (all d: 0.4680; closed form: 0.4691).

Rule A of the prereg is a VERIFICATION, not an adjudication: with a rank-one field the exponent is algebraically pinned to 1/2 (prereg Sec. 3), because the pathwise error variance is d-independent by construction and the zeroth-order one is `(d-1)*const + const`. A value outside `[0.35, 0.65]`, or a c* away from the closed form, indicates an implementation bug -- not a refutation of Claim 4.


## 4. E1b -- the full-estimator crossover

| d | c* (E1a, error-only) | c* (E1b, full MSE) | ratio |
|---|---|---|---|
| 1 | 1.2014 | 43.3751 | 36.104 |
| 2 | 1.5182 | 52.1301 | 34.337 |
| 4 | 2.0495 | 66.1693 | 32.285 |
| 8 | 2.8746 | 87.9481 | 30.595 |
| 16 | 4.0603 | 120.4022 | 29.653 |
| 32 | 5.7465 | 166.7252 | 29.013 |
| 64 | 8.1306 | 233.0598 | 28.664 |

Fitted exponent for E1b: **p = 0.4359**.

E1a and E1b need not agree, and here they differ by a factor of ~29-36. The gap is the smooth-part variance the zeroth-order operator pays on `Q^pi`, which the error term must overcome before the ranking can flip.

**This crossover is not an intrinsic threshold.** Setting `(eps/sigma)^2 c^2 / (2M) ~ s1_zo` gives `c* ~ 1/eps`: the practical crossover moves with how large the critic error is relative to the Q spread. Section 7.1b measures exactly that 1/eps scaling. So E1b answers "how wrong must the critic be, and how fast must its error oscillate, before the zeroth-order operator wins" -- two knobs, not one. Only E1a's contour is a property of the operators alone.


## 5. Cross-term reconciliation (spec Sec. 5.2)

Claim 4's decomposition drops `2 Cov(g[Q^pi], dg)`. Registered threshold: if `|2 Cov| > 0.25 Var_e` anywhere in the swept region, the decomposition is flagged as incomplete in the paper.

Evaluated AT the crossover, not maximised over the grid: as `c -> 0` the error term vanishes as `c^2` while the cross term vanishes as `c`, so a grid maximum diverges in cells where both are negligible against the smooth part. That is division by something going to zero, not a property of Claim 4.

| d | \|2Cov\|/Var_e at crossover (PW) | (ZO) | over 0.25 threshold |
|---|---|---|---|
| 1 | 0.5091 | 6.6390 | **YES** |
| 2 | 0.1650 | 14.7032 | **YES** |
| 4 | 0.0687 | 3.7908 | **YES** |
| 8 | 0.0952 | 0.2329 | no |
| 16 | 0.0084 | 0.1614 | no |
| 32 | 0.0017 | 0.1149 | no |
| 64 | 0.0050 | 0.0948 | no |

**The dropped cross term is not negligible at d = [1, 2, 4].** For those dimensions Claim 4's variance decomposition is incomplete as stated, and the registered response is to flag it in the paper rather than leave it in a drawer. It falls below the threshold for the remaining dimensions.

This matters for how the DMC evidence is read: the paper's two action dimensions are 6 and 21, and the boundary found here sits between d = 4 and d = 8.


## 6. Bootstrap and variance decomposition

| resampling | p | sd | 95% CI | resamples |
|---|---|---|---|---|
| states + batches (registered) | 0.4876 | 0.00034 | [0.4869, 0.4883] | 10000 |
| states only, batches frozen | 0.4876 | 0.00026 | [0.4871, 0.4881] | 3000 |
| batches only, states frozen | 0.4876 | 0.00022 | [0.4872, 0.4880] | 3000 |

**Variance decomposition by level**: total 1.161e-07; state level 6.736e-08 (58%); batch level 4.849e-08 (42%).

Both levels contribute comparably, so the interval is not merely an artefact of the replicate count N. Had the batch level dominated, the CI would have been a knob rather than evidence about `p`, and this table is what shows which case obtains.


## 7. Robustness -- and the falsifiable rule

Rule A above is a verification. **Rule B is the falsifiable one**: for rank > 1 there is no closed form pinning the exponent, so the rank ladder is where Claim 4 can actually fail.


### 7.1 Rank ladder -- Rule B

`c*` is measured against the NOMINAL omega. The realised frequency under the registered primary convention is `omega_inf = omega / sqrt(r)`, so a ladder whose rank is FIXED shifts nothing, while a ladder with `r = d` shifts the exponent by `-1/2`.

| arm | rank | p (nominal omega) | p (omega_inf, registered primary) |
|---|---|---|---|
| rank-one | 1 | 0.4876 | **0.4876** |
| r = 2 | 2 | 0.4870 | **0.4870** |
| full rank | d | 0.4871 | **-0.0129** |

The nominal exponent is ~0.487 at EVERY rank: Claim 4's mechanism is real and rank-independent. What changes is the exponent in the quantity Claim 4 is written in. With the error field's rank growing as `d`, the `1/sqrt(r)` in `omega_inf` exactly cancels the `sqrt(d)`, leaving a **dimension-independent threshold**.

**Rule B: REFUTED** at full rank (p = -0.0129, outside both the `[0.35, 0.65]` and `[0.8, 1.2]` bands). Per the registered rule, the dimensional prediction is withdrawn from the abstract and the rank dependence is reported in its place. Rule B is CONFIRMED at fixed rank (0.4876 at r=1, 0.4870 at r=2).


The precise statement the data supports: **`sigma * omega > sqrt(d)` holds when the critic error field has fixed rank, and becomes `sigma * omega_inf > const` when its rank grows with the action dimension.** Which regime describes a real critic is not tested here -- that is E3, and it is now the load-bearing question for this part of the paper.


### 7.1b eps-invariance (what it does and does not test)

The E1a error-only statistics are accumulated with `eps` factored out analytically, so the E1a contour is eps-independent BY CONSTRUCTION rather than by measurement, and this arm confirms the implementation, not the mathematics. (The two arms were run at different N -- 10^4 and 2x10^3 -- so their E1a contours agree to Monte Carlo error rather than exactly.) The substantive eps-dependence is in E1b, where `eps` enters the reconstruction.

| d | c* E1b @ eps=0.05 | c* E1b @ eps=0.20 | ratio (4x eps predicts 4.00) |
|---|---|---|---|
| 1 | 43.3751 | 10.8406 | 4.001 |
| 2 | 52.1301 | 13.0508 | 3.994 |
| 4 | 66.1693 | 16.5238 | 4.004 |
| 8 | 87.9481 | 22.0059 | 3.997 |
| 16 | 120.4022 | 30.2100 | 3.986 |
| 32 | 166.7252 | 41.9141 | 3.978 |
| 64 | 233.0598 | 59.0075 | 3.950 |

The ratio is 4.00 to three digits at every d: the E1b crossover scales exactly as `1/eps`, confirming it is a joint statement about error amplitude and error frequency rather than a threshold on frequency alone.


### 7.2 The norm convention decides the sign of the exponent

`c*` is measured against the NOMINAL omega. The realised frequency differs by a rank-dependent factor, so the reported exponent depends on which norm Claim 4 means. For a rank-r field: `||e||_inf = eps sqrt(r)`, `sup_a ||grad e||_2 = eps omega`, `max_j sup_a |d_j e| = eps omega / sqrt(r)`.

| convention | omega_inf / omega | p at rank r = d |
|---|---|---|
| `sup ||grad e||_2 / ||e||_inf` (**registered primary**) | `1/sqrt(d)` | nan |
| `max_j sup |d_j e| / ||e||_inf` | `1/d` | nan |

The two readings differ by a full unit in the exponent. Until the paper states which norm `omega` denotes, Claim 4 is not well-posed for a full-rank error field -- and that holds whether Rule B confirms or refutes.


### 7.3 Arm 3: the operator that actually ships

`actor_update_mode="weighted_mle"` does NOT optimise the centred estimator of Claim 4. It maximises a softmax-weighted MLE whose mean displacement is `sum_i w_i u_i`, `w = softmax(Q/eta)`. Cosine to the exact blurred estimand, since the shipped operator's magnitude is set by eta and not by ||grad Q||.

| d | cos(g_PW, g*) | cos(g_ZO, g*) | cos(d_ESTEP, g*) | ESS / M | ||d_ESTEP||/||g_ZO|| |
|---|---|---|---|---|---|
| 1 | 0.7758 | 0.8666 | 0.8709 | 18.3/32 | 79.160 |
| 2 | 0.8243 | 0.8808 | 0.8258 | 18.2/32 | 16.477 |
| 4 | 0.8289 | 0.8356 | 0.7862 | 17.3/32 | 18.047 |
| 8 | 0.8310 | 0.7706 | 0.6766 | 16.0/32 | 18.700 |
| 16 | 0.8086 | 0.6475 | 0.5295 | 15.7/32 | 19.131 |
| 32 | 0.7754 | 0.5036 | 0.3818 | 14.9/32 | 23.305 |
| 64 | 0.7645 | 0.3887 | 0.2749 | 14.7/32 | 21.848 |

If `d_ESTEP` tracks `g_ZO`, the theory describes the code. If it tracks neither, Claim 4 is a statement about an estimator the algorithm does not use, and the paper must say so.


## 8. Limitations

1. **The rank-one exponent is algebraic.** See prereg Sec. 3. E1a verifies the pipeline and the production estimator core against a closed form; it is not independent evidence for Claim 4's dimensional prediction.

2. **Claim 4 is not well-posed for a full-rank error field.** `||e||_inf` and `||grad e||` scale differently with rank, so the realised `omega_inf` is `omega/sqrt(r)` under the L2 reading and `omega/r` under the componentwise one -- fitted exponents of 0 and -0.5 against +0.5. The paper must state which norm `omega` means.

3. `Q^pi` is quadratic and globally smooth; a neural critic is neither. The error is planted, not learned. This licenses nothing about DMC.

4. `sigma < 0.1` is out of reach for the DMC arms (effective `min_std` is 0.1, hardcoded in `src/networks/jax_models.py`).

